export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user;

export const selectRecipes = (state) => state.user.recipes;


export const selectFavouritesRecipes = (state) => state.user.recipes;

